export const server = {
  enabled: true,
  server: '', // If it is '', connect to localhost
  port: 8001
}

export const logger = {
  // logging
}

export const config = {
  server,
  logger
}

export default config
