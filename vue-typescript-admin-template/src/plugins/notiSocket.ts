import _Vue from 'vue'
import io from 'socket.io-client'

export default function notiSocketPlugin (Vue: typeof _Vue): void {
  // const notiSocket: SocketIOClient.Socket = io('http://localhost:8002/notification', {
  //   query: {
  //     123: '123'
  //   }
  // })

  // Vue.prototype.$notiSocket = notiSocket
}
