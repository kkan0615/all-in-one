import Vue from 'vue'
import Vuetify from 'vuetify/lib'

Vue.use(Vuetify)

export const vuetify = new Vuetify({
  theme: {
    dark: true
    // themes: {
    //   dark: {

    //   }
    // }
  }
})

export default vuetify
