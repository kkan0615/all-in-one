
import { GetterTree } from 'Vuex'

const getters: GetterTree<string, unknown> = {
  // roles: (state) => state.user.roles,
  // routes: (state) => state.permission.routes,
  // customerId: (state) => state.user.id.customer,
  // userId: (state) => state.user.id.user,
  // userName: (state) => state.user.userName,
  // accessIp: (state) => state.user.accessIp,
  // serverSocket: (state) => state.socket.serverSocket,
  // devices: (state) => state.cache.devices,
  // alarms: (state) => state.cache.alarms,
  // tags: (state) => state.cache.tags
  // // devices: state => state.device.listeners
}
export default getters
